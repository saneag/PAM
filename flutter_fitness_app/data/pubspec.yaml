name: data
description: A new Flutter package project.
version: 0.0.1
# homepage:

environment:
  sdk: '>=2.18.1 <3.0.0'

dependencies:
  domain:
    path: ../domain
  json_annotation:
  http:

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
  json_serializable:
  build_runner:

flutter:
  assets:
    - ../presentation/resources/files/

